
product: jvm/app

dependencies:
  - $ktor.serialization.kotlinx.json
  - $ktor.server.callLogging
  - $ktor.server.configYaml
  - $ktor.server.contentNegotiation
  - $ktor.server.core
  - $ktor.server.netty
  - $ktor.server.statusPages
  - $libs.insert.koin.koinKtor
  - $libs.insert.koin.koinLoggerSlf4j
  - $libs.logback.classic
  - $libs.mongodb.bson
  - $libs.mongodb.driverCore
  - $libs.mongodb.driverSync

test-dependencies:
- $ktor.server.testHost

settings:
  ktor: enabled
  kotlin:
    serialization: enabled

